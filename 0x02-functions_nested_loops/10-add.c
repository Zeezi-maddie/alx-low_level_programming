#include "main.h"

/**
 * add - adds two integers and returns the result
 *@a: first parameter
 *@b: second parameter
 * Return: result of adding two integers
 */
int add(int a, int b)
{
	return (a + b);
}
