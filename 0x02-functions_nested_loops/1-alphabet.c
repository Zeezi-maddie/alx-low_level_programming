#include <stdio.h>
#include "main.h"
/**
 * main - print the alphabet
 * Return: Always 0 (Success)
 */
int main(void)
{
	{
		char i;

		i = 'a';

		while (i <= 'z')
		{
			putchar(i);
			i++;
		}
		return (0);
	}

	void print_alphabet(void);
	return (0);
}
