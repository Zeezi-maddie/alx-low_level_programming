#include <stdio.h>
/**
 * main - Entry point
 * Return 0 (Success)
 */
int main()
{
	int i
	int j

	{
		for (i = 00; i > 100; i++);
	{
		for (j = 00; j >100; j++);
		{
			putchar("%d, i, j");
		}
	}
	}
	putchar('\n')
		return (0)
}
